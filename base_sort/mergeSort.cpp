#include <iostream>
#include <vector>
#include <math>

using namespace std;

vector<int> mergePartial(vector<int> ts){
    
    return ts;
}

vector<int> mergeSort(int* ts){
    int total_len = ts.size();
    int middle = math.floor(total_len/2);
    if (middle>=2){
        mergeSort(ts)
    }
    return ts;
}